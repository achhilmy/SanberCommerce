import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const Photo = () => {
    return (
        <View>
            <Text>Atom Component Photo</Text>
        </View>
    )
}

export default Photo;

const styles = StyleSheet.create({})
